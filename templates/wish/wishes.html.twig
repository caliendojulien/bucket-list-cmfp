{% extends 'base.html.twig' %}

{% block body %}
    {% for wish in wishes %}
        <a href="{{ path('wish_wish', {'wish': wish.id}) }}">
            {{ wish.title }} - {{ wish.category.name }}
        </a>
    {% endfor %}
{% endblock %}

{% block title %}

{% endblock %}

